const mode =document.getElementById("modo-icon");
mode.addEventListener("click",() => {
    if (mode.classList.contains("./img/icons8 - lua - cheia - 48.png"))
    mode.classList.add();
    return;

}
 mode.classList.add():
 if (mode.classList.contains("./img/icons8 - lua - cheia - 48.png"))
 }}: